1. Too many console.logs => can be put into a display function
  getStartPosition(), getKey()
2. updateGrid() => can be renamed
3. 142 can be put into display function
4. console.clear can be put into a function